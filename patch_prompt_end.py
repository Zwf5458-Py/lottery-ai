import re

with open('modules/ai_engine.py', 'r', encoding='utf-8') as f:
    content = f.read()

c5_target = """    # ====== 组合最终 Prompt ======
    recent_detail_block = f"{recent_detail}" if recent_detail else ""
    prompt = f\"\"\"你是一位资深的彩票统计分析专家。请根据以下"{type_name}"的历史统计数据进行分析。

{zmap_text}

⚠️ 重要约束：
1. 用户勾选了以下加权维度进行分析：【{active_dims_text}】。禁止提及或使用未选择的维度。
2. 极其重要：以下所有统计数据全部【仅针对特码】统计，因此你必须将所有已选维度的数据全部用于推荐【特码】。
3. 推荐【正码】（6个）时，采用均衡分布策略即可，不要用特码的统计数据来选正码。
4. 特码选取必须严格基于已选维度的数据做出有据可依的推荐，千万不要发散到未选的走势分析上。
5. 当你提到号码对应的生肖时，绝对！绝对！绝对要参照上方提供的【系统设定：1-49生肖精确映射】，切勿使用你内置的过期生肖知识。

## 最近 300 期原始特码序列（从旧到新，请自行发现隐藏规律）
{raw_special_nums}

{recent_detail_block}

## 已选维度统计数据

{data_block}

## 分析要求
1. 🛑 极其严格的维度约束：你【必须且只能】分析我提供给你的维度数据。如果用户没有勾选某个维度（比如没有提供大小、单双或冷热数据），你【绝对禁止】在分析中自己凭空捏造、猜测或补充该维度的分析。不要产生“幻觉”！
2. 根据选定的各个维度相互印证，综合锁定最可能跃迁的特码区间。
3. 🔍 自主发现：请仔细观察上方原始特码序列，尝试发现我未定义的隐藏规律，例如：
   - 号码间隔/跳跃规律（相邻两期差值是否有周期性）
   - 🎨 波色跳跃与遗漏规律（例如：连续开出蓝波后打断跳红波，或某波色遗漏10期以上极易反弹）
   - 某些号码是否有固定回头周期（如每隔N期重复出现）
   - 区间偏好（连续多期集中在某个十位段）
   将你发现的任何额外规律融入推荐理由中。
4. 📊 多模态视觉超能力：我已在附件中提供了一张近期的【特码生肖走势折线图点阵图】。请你发挥视觉大模型的多模态能力，观察图表中的连线波动（比如是否存在头肩顶、波峰波谷的三重对称、大级别M头等技术图形），把这种图形周期叠加进最终号码选取结论中。
5. 正码6个，特码1个，范围均为 1-49，正码不可重复，但特码可与正码相同。
6. 📖 深度长篇推理（建议 800-1500 字）：请不要惜字如金！请写出一篇详细深入的分析推理长文，详细拆解推理的每个环节：哪些是基于已选维度的推断、规律的共振互补，哪些是你自主发现或通过视觉图表看出的多模态图形规律。让过程推演成为一篇极具说服力的分析报告。

请严格以如下 JSON 格式回复：
{{
    "numbers": [正码1, 正码2, 正码3, 正码4, 正码5, 正码6],
    "special_num": 特码,
    "analysis": "你的分析理由（分别说明正码和特码的逻辑，只讨论所选维度）",
    "confidence": "高/中/低"
}}\"\"\"
    
    return prompt"""

c5_replace = """    # ====== 内部数学引擎打分权重披露（迫使AI严密贴合图表规则） ======
    weights_info = ""
    if system_weights:
        weights_info = f\"\"\"
## 底层图表算法赋予的核心权重系数参考（数值>1代表看好，<1代表看衰）：
- 大号权重: {system_weights.get('big_weight', 1.0):.2f}，小号权重: {system_weights.get('small_weight', 1.0):.2f}
- 单号权重: {system_weights.get('odd_weight', 1.0):.2f}，双号权重: {system_weights.get('even_weight', 1.0):.2f}
- 若本期核心【{pre_sel_special}号】被选中，其底层冷热加权: {system_weights.get('hot_cold_weights', {{}}).get(pre_sel_special, 1.0):.2f}
- 若本期核心【{pre_sel_special}号】被选中，其底层尾数加权: {system_weights.get('tail_weights', {{}}).get(pre_sel_special % 10, 1.0):.2f}
\"\"\"

    # ====== 组合最终 Prompt ======
    recent_detail_block = f"{recent_detail}" if recent_detail else ""
    prompt = f\"\"\"你是一位资深的彩票走势分析专家与系统推理报告撰写者。

【系统高度机密指令】：底层的工业级统计算法（含马尔可夫、贝叶斯极值反弹、LSTM、高级RLE防连跳等）已经高度结合了图表规则，**精准计算并选定了本期的号码**。
你的唯一任务是：作为系统的“首席分析师”，根据下方的【历史走势数据】与【底层权重系数】，写一篇严丝合缝、极具逻辑说服力的长篇图表规则报告，向用户解释**底层数学模型基于图表为什么推算出了这组号码**，从而彻底消除你的逻辑幻觉，完美匹配图表。

👉 **本期底层算法最终敲定的【特码】：{pre_sel_special}号**
👉 **本期底层算法最终敲定的【正码配置】：{pre_sel_nums}**

{zmap_text}

⚠️ 绝不妥协的纪律要求：
1. 目标锁定：你最后的分析结论【必须、无可争议地】指向特码 {pre_sel_special}，逻辑演绎必须闭环于此号码。正码 {pre_sel_nums} 也必须在报告中合理提及其均衡分布特征。
2. 唯图表论：用户只勾选了【{active_dims_text}】。你必须只用这些维度来圆你的逻辑。绝不能瞎编未勾选的理由。
3. 杜绝对数据的数值幻觉：你在分析中引用的任何历史连跳次数、概率百分比，**必须**从下方的《已选维度统计数据》中提取！如果数据里没提，就不要自己瞎编具体数字（如不要乱编“K线为[-1,1...]”这种不存在的数据）。

## 最近 300 期原始特码序列（从旧到新，供你挖掘隐藏视觉规律）
{raw_special_nums}

{recent_detail_block}

## 已选维度统计数据与底层推算中间变量
{data_block}
{weights_info}

## 分析撰写结构要求（参考字数 800 - 1500 字）
请按以下模块化结构撰写这篇极品分析报告（格式自行美化，可使用加粗等）：
1. 宏观盘面与走势图景：结合最近走势和多维开奖信号。
2. 图表规则剥茧：例如如何通过 RLE 的跳动规律判断大小单双必反转或连庄；如何通过贝叶斯数据看到大冷回补。
3. 底层计算逻辑解码：带入上述提供的“权重系数参考”，解释为何算法在各个维度给予的方向倾向。
4. 多维共振导向 {pre_sel_special} 号：结合生肖、尾数、波色、冷热，完美综合论证为何根据图表规则，{pre_sel_special}号是当前的最强计算解。
5. 正码策略简析：简述 {pre_sel_nums} 作为均衡搭配的合理性。

请严格以如下 JSON 格式回复（将你的长篇推算报告置于 analysis 字段，号码原样回传）：
{{
    "numbers": {pre_sel_nums},
    "special_num": {pre_sel_special},
    "analysis": "你的严密推算报告...",
    "confidence": "高"
}}\"\"\"
    
    return prompt"""

content = content.replace(c5_target, c5_replace)

with open('modules/ai_engine.py', 'w', encoding='utf-8') as f:
    f.write(content)

print("Prompt chunk patched.")
